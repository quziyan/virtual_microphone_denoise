.venv/
__pycache__/
*.pyc
/tmp/
mic_raw.wav
mic_denoised.wav
*_cleaned.wav

recordings/
web/__pycache__/

build/
*.egg-info/

# dist/ is tracked for downloads, but only the installer + changelog —
# not the PyInstaller build intermediates.
dist/*
!dist/VibeCodingVirMic-Installer-*.pkg
!dist/README.md

# local tooling / macOS / regenerable build intermediates
.claude/
.DS_Store
packaging/AppIcon.iconset/

# telemetry credentials — bundled into the built .app, NEVER committed
src/reporter_config.py

# kept and maintained locally only — not in the remote repo
CLAUDE.md
docs/wechat-article.md
docs/wechat-article-boost.md
docs/wechat-article-knowledge.md
